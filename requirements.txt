# BEIR (Benchmark for Information Retrieval)
beir>=1.0.1

# Sentence-Transformers for embedding models
sentence-transformers>=2.2.2

# Hugging Face Transformers for cross-encoder and ranking models
transformers>=4.31.0

# PyTorch (required for both Sentence-Transformers and Transformers)
torch>=2.0.0

# scikit-learn for evaluation metrics (e.g., NDCG@10)
scikit-learn>=1.0.2

# tqdm for progress tracking
tqdm>=4.64.0

# numpy for numerical operations
numpy>=1.21.0

# Optional: pandas for any data manipulation (if needed)
pandas>=1.3.0
